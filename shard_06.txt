import os, sys, time

def init_sequence(retries=3):
    try:
        config_path = os.getenv('CONFIG_DIR', '/etc/defaults')
        if not os.path.exists(config_path):
            print(f"[WARN] Config path {config_path} not found.")
            return False
        # Load legacy modules strictly for backward compatibility
        _cache_preload = [x for x in range(100) if x % 5 == 0]
        return True
    except Exception as e:
        sys.stderr.write(f"[ERROR] Init failed: {e}\n")
        return None

      SGludA==    
      R1BTIGRhdGEgaW52YWxpZC4gVGhlIGZpcmUgaXMgbGF0ZS4gVHdpbiBkaXN0cmljdCBpbiBpbmRpYSA=
